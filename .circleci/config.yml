version: 2.1

parameters:
  run_ios_ci:
    type: string
    default: "false"
  test_impact_base:
    type: string
    default: origin/main
  test_impact_mode:
    type: enum
    enum: [shadow, enforce]
    default: shadow

executors:
  m4-pro-medium:
    macos:
      xcode: "27.0"
    resource_class: m4pro.medium
    shell: /bin/bash --login -eo pipefail
    environment:
      # Tool installation is required; local-development agent skills are not.
      MISE_NO_HOOKS: "1"
  m4-pro-large:
    macos:
      xcode: "27.0"
    resource_class: m4pro.large
    shell: /bin/bash --login -eo pipefail
    environment:
      MISE_NO_HOOKS: "1"

commands:
  hydrate_snapshot_references:
    steps:
      - run:
          name: Hydrate snapshot references
          command: |
            git lfs pull
            pointer_files="$(git grep -Il '^version https://git-lfs.github.com/spec/v1$' -- ':(glob)**/__Snapshots__/**/*.png' || true)"
            if [[ -n "$pointer_files" ]]; then
              echo "Snapshot references remain as Git LFS pointers:" >&2
              printf '%s\n' "$pointer_files" >&2
              exit 1
            fi

  configure_test_workdir:
    parameters:
      test_workdir:
        type: string
    steps:
      - run:
          name: Configure test work directory
          command: |
            printf '%s\n' 'export TEST_WORKDIR="$CIRCLE_WORKING_DIRECTORY/<< parameters.test_workdir >>"' >> "$BASH_ENV"
            mkdir -p "$CIRCLE_WORKING_DIRECTORY/<< parameters.test_workdir >>"
            chmod 700 "$CIRCLE_WORKING_DIRECTORY/<< parameters.test_workdir >>"

  test_ci_helpers:
    steps:
      - run:
          name: Test CI helpers
          command: |
            python3 .circleci/test_xcresult_to_junit.py
            python3 .circleci/test_test_artifacts.py
            python3 .circleci/test_test_impact.py
            python3 Tools/Tests/test_snapshot_shards.py
            ./snapshot-shards check

  validate_runner_environment:
    steps:
      - run:
          name: Validate runner environment
          command: |
            echo "CPU: $(sysctl -n machdep.cpu.brand_string)"
            echo "Logical CPUs: $(sysctl -n hw.logicalcpu)"
            echo "Memory bytes: $(sysctl -n hw.memsize)"
            echo "Architecture: $(uname -m)"
            sw_vers
            xcodebuild -version
            echo "Available simulator runtimes:"
            xcrun simctl list runtimes available

            xcode_build="$(xcodebuild -version | awk '/Build version/{print $3}')"
            if [[ "$xcode_build" != "27A5228h" ]]; then
              echo "Expected Xcode build 27A5228h, found $xcode_build" >&2
              exit 1
            fi
            if ! xcrun simctl list runtimes available | grep -Eq '^iOS 27(\.0)? '; then
              echo "An available iOS 27 simulator runtime is required" >&2
              exit 1
            fi

            selected_simulator="$(./simulator --shared --os 27.0 --no-boot)"
            echo "Selected simulator: $selected_simulator"
            xcrun simctl list devices available | grep "$selected_simulator"

            echo "Checkout: $(git rev-parse HEAD)"
            echo "Origin: $(git remote get-url origin)"
            if git config --local --get-regexp 'credential|extraheader' >/tmp/git-credential-config.txt 2>/dev/null; then
              echo "Unexpected persisted Git credential configuration:" >&2
              sed -E 's/(authorization:).*/\1 [REDACTED]/I' /tmp/git-credential-config.txt >&2
              exit 1
            fi

  prepare_builder:
    parameters:
      test_workdir:
        type: string
    steps:
      - checkout
      - restore_cache:
          keys:
            - mise-installs-v2-{{ arch }}-{{ checksum ".mise.toml" }}
      - restore_cache:
          keys:
            - swiftpm-repositories-v2-{{ arch }}-{{ checksum "Package.resolved" }}
      - restore_cache:
          keys:
            - swiftpm-artifacts-v2-{{ arch }}-{{ checksum "Package.resolved" }}
      - restore_cache:
          keys:
            - test-impact-v1-{{ arch }}-{{ checksum "Package.resolved" }}
      - run:
          name: Install pinned tools
          command: |
            if ! command -v mise >/dev/null 2>&1; then
              brew install mise
            fi
            mise trust .mise.toml
            mise install
      - save_cache:
          key: mise-installs-v2-{{ arch }}-{{ checksum ".mise.toml" }}
          paths:
            - ~/.local/share/mise/installs
      - configure_test_workdir:
          test_workdir: << parameters.test_workdir >>
      - test_ci_helpers
      - run:
          name: Test compiler-index selection
          command: >-
            STUFF_TEST_IMPACT_ONLY=1 swift test
            --scratch-path .build/test-impact-tool
            --filter TestImpactToolTests
      - save_cache:
          key: test-impact-v1-{{ arch }}-{{ checksum "Package.resolved" }}
          paths:
            - .build/test-impact-tool
      - run:
          name: Generate project and resolve Swift packages
          command: |
            # Populate the immutable download caches before either test job saves them.
            started="$(python3 -c 'import time; print(time.time_ns())')"
            if mise exec -- tuist generate --no-open >"$TEST_WORKDIR/generate.log" 2>&1; then
              python3 -c 'import json,sys,time; print("CI_TIMING " + json.dumps({"phase":"generate","seconds":round((time.time_ns()-int(sys.argv[1]))/1e9,3),"status":0}, separators=(",",":")))' "$started"
            else
              status=$?
              python3 -c 'import json,sys,time; print("CI_TIMING " + json.dumps({"phase":"generate","seconds":round((time.time_ns()-int(sys.argv[1]))/1e9,3),"status":int(sys.argv[2])}, separators=(",",":")))' "$started" "$status"
              echo "error: tuist generate failed. Tail of $TEST_WORKDIR/generate.log:" >&2
              tail -n 20 "$TEST_WORKDIR/generate.log" >&2
              exit "$status"
            fi
      - save_cache:
          key: swiftpm-repositories-v2-{{ arch }}-{{ checksum "Package.resolved" }}
          paths:
            - ~/Library/Caches/org.swift.swiftpm/repositories
      - save_cache:
          key: swiftpm-artifacts-v2-{{ arch }}-{{ checksum "Package.resolved" }}
          paths:
            - ~/Library/Caches/org.swift.swiftpm/artifacts
      - validate_runner_environment

  prepare_worker:
    parameters:
      hydrate_snapshots:
        type: boolean
        default: false
      scheme:
        type: string
      test_workdir:
        type: string
    steps:
      - checkout
      - when:
          condition: << parameters.hydrate_snapshots >>
          steps:
            - hydrate_snapshot_references
      - attach_workspace:
          at: ci-test-build
      - configure_test_workdir:
          test_workdir: << parameters.test_workdir >>
      - test_ci_helpers
      - run:
          name: Validate test artifact manifest
          command: >-
            python3 .circleci/test_artifacts.py resolve
            --root "$CIRCLE_WORKING_DIRECTORY/ci-test-build"
            --scheme "<< parameters.scheme >>" --field xctestrun
      - run:
          name: Validate test selection
          command: >-
            python3 .circleci/test_impact.py validate
            --selection "$CIRCLE_WORKING_DIRECTORY/ci-test-build/test-impact/selection.json"
            --scheme "<< parameters.scheme >>"
      - validate_runner_environment

  run_with_timeout:
    parameters:
      seconds:
        type: integer
      command:
        type: string
    steps:
      - run:
          name: Run tests
          no_output_timeout: 45m
          command: |
            ruby -e '
              timeout = Integer(ARGV.shift)
              started = Process.clock_gettime(Process::CLOCK_MONOTONIC)
              pid = Process.spawn(*ARGV, pgroup: true)
              loop do
                reaped = Process.waitpid(pid, Process::WNOHANG)
                if reaped
                  status = $?
                  exit(status.exitstatus || 128 + status.termsig)
                end
                if Process.clock_gettime(Process::CLOCK_MONOTONIC) - started >= timeout
                  warn "error: test command exceeded #{timeout} seconds"
                  begin Process.kill("TERM", -pid); rescue Errno::ESRCH; end
                  sleep 10
                  begin Process.kill("KILL", -pid); rescue Errno::ESRCH; end
                  begin Process.wait(pid); rescue Errno::ECHILD; end
                  exit 124
                end
                sleep 1
              end
            ' << parameters.seconds >> /bin/bash -lc '<< parameters.command >>'

  export_test_results:
    parameters:
      test_workdir:
        type: string
      output_directory:
        type: string
      assigned_file:
        type: string
        default: ""
    steps:
      - run:
          name: Convert Xcode results to JUnit
          when: always
          command: |
            mkdir -p "<< parameters.output_directory >>"
            python3 .circleci/xcresult_to_junit.py \
              "<< parameters.test_workdir >>" \
              "<< parameters.output_directory >>/results.xml"
      - store_test_results:
          path: << parameters.output_directory >>

  audit_test_impact:
    parameters:
      scheme:
        type: string
      junit:
        type: string
      output_directory:
        type: string
      assigned_file:
        type: string
        default: ""
      mode:
        type: string
        default: shadow
    steps:
      - run:
          name: Audit test selection
          when: always
          command: |
            assigned_args=()
            if [[ -n "<< parameters.assigned_file >>" ]]; then
              assigned_args+=(--assigned-file "<< parameters.assigned_file >>")
            fi
            python3 .circleci/test_impact.py audit \
              --selection "$CIRCLE_WORKING_DIRECTORY/ci-test-build/test-impact/selection.json" \
              --scheme "<< parameters.scheme >>" \
              --junit "<< parameters.junit >>" \
              --output "<< parameters.output_directory >>/report.json" \
              --mode "<< parameters.mode >>" \
              "${assigned_args[@]}"
      - store_artifacts:
          path: << parameters.output_directory >>
          destination: test-impact

  collect_diagnostics:
    parameters:
      test_workdir:
        type: string
      artifact_name:
        type: string
      include_snapshot_images:
        type: boolean
        default: false
    steps:
      - run:
          name: Collect diagnostics
          when: on_fail
          command: |
            mkdir -p diagnostics/crashes
            cp -R "$HOME/Library/Logs/DiagnosticReports/." diagnostics/crashes/ 2>/dev/null || true
            cp -R "<< parameters.test_workdir >>" diagnostics/test-output
            if << parameters.include_snapshot_images >>; then
              mkdir -p diagnostics/snapshot-images
              { grep -hEo '/[^[:space:]]+\.png' "<< parameters.test_workdir >>"/*.log 2>/dev/null || true; } \
                | sort -u \
                | while IFS= read -r image; do
                    [[ -f "$image" ]] || continue
                    destination="diagnostics/snapshot-images/${image#/}"
                    mkdir -p "$(dirname "$destination")"
                    cp "$image" "$destination"
                  done
            fi
            find diagnostics "<< parameters.test_workdir >>" -maxdepth 3 | head -200
      - store_artifacts:
          path: diagnostics
          destination: << parameters.artifact_name >>/diagnostics

jobs:
  build-ios-tests:
    executor: m4-pro-large
    environment:
      TEST_IMPACT_BASE: << pipeline.parameters.test_impact_base >>
    steps:
      - prepare_builder:
          test_workdir: test-output-build
      - run:
          name: Test backup upgrader
          command: mise exec -- ruby Where/Tools/Tests/upgrade_backup_test.rb
      - run_with_timeout:
          seconds: 2700
          command: >-
            ./test --everything
            --build-artifacts "$CIRCLE_WORKING_DIRECTORY/ci-test-build"
            --no-generate --skip-architecture
      - run:
          name: Compute test selection
          command: |
            ./test-impact select \
              --base "$TEST_IMPACT_BASE" \
              --index-store "$CIRCLE_WORKING_DIRECTORY/ci-test-build/DerivedData/Index.noindex/DataStore" \
              --output "$CIRCLE_WORKING_DIRECTORY/ci-test-build/test-impact"
            ./test-impact explain \
              --selection "$CIRCLE_WORKING_DIRECTORY/ci-test-build/test-impact/selection.json"
      - run:
          name: Validate changed snapshot inventory with Xcode
          command: |
            if git diff --quiet origin/main...HEAD -- \
              Project.swift ':(glob)**/SnapshotTests/**/*.swift'; then
              echo "Snapshot definitions did not change. The source-plan validation is sufficient."
              exit 0
            fi
            ./test --snapshots --shared --os 27.0 \
              --test-artifacts "$CIRCLE_WORKING_DIRECTORY/ci-test-build" \
              --enumerate-suites "$TEST_WORKDIR/suites.txt" \
              --skip-architecture
            ./snapshot-shards compare-enumeration \
              --enumeration "$TEST_WORKDIR/suites.txt"
      - persist_to_workspace:
          root: ci-test-build
          paths:
            - manifest.json
            - DerivedData/Build/Products
            - test-impact
      - collect_diagnostics:
          test_workdir: test-output-build
          artifact_name: build-ios-tests

  test-ios:
    executor: m4-pro-medium
    environment:
      TEST_IMPACT_MODE: << pipeline.parameters.test_impact_mode >>
    steps:
      - prepare_worker:
          scheme: Stuff-iOS-Tests
          test_workdir: test-output-ios
      - run:
          name: Materialize unit test selection
          command: |
            python3 .circleci/test_impact.py materialize \
              --selection "$CIRCLE_WORKING_DIRECTORY/ci-test-build/test-impact/selection.json" \
              --scheme Stuff-iOS-Tests \
              --output "$TEST_WORKDIR/selected.txt"
      - run_with_timeout:
          seconds: 2700
          command: |
            selection_args=()
            if [[ "$TEST_IMPACT_MODE" == enforce ]]; then
              if [[ ! -s "$TEST_WORKDIR/selected.txt" ]]; then
                echo "Compiler-index selection contains no unit suites; skipping simulator execution."
                exit 0
              fi
              selection_args+=(--only-file "$TEST_WORKDIR/selected.txt")
            fi
            ./test --all --shared --os 27.0 \
              --test-artifacts "$CIRCLE_WORKING_DIRECTORY/ci-test-build" \
              --skip-architecture "${selection_args[@]}"
      - export_test_results:
          test_workdir: test-output-ios
          output_directory: test-results-ios
      - audit_test_impact:
          scheme: Stuff-iOS-Tests
          junit: test-results-ios/results.xml
          output_directory: test-impact-ios
          mode: << pipeline.parameters.test_impact_mode >>
      - collect_diagnostics:
          test_workdir: test-output-ios
          artifact_name: test-ios

  snapshot:
    executor: m4-pro-medium
    parallelism: 3
    environment:
      SNAPSHOT_SETTLE_TIMEOUT_MULTIPLIER: "2"
      TEST_IMPACT_MODE: << pipeline.parameters.test_impact_mode >>
    steps:
      - prepare_worker:
          hydrate_snapshots: true
          scheme: StuffSnapshotTests
          test_workdir: test-output-snapshots
      - run:
          name: Select deterministic snapshot shard
          command: |
            shard="$((CIRCLE_NODE_INDEX + 1))"
            ./snapshot-shards list "$shard" --total "$CIRCLE_NODE_TOTAL" \
              > "$TEST_WORKDIR/shard.txt"
            suite_count="$(wc -l < "$TEST_WORKDIR/shard.txt" | tr -d ' ')"
            printf 'CI_SHARD {"index":%s,"shards":%s,"suites":%s}\n' \
              "$CIRCLE_NODE_INDEX" "$CIRCLE_NODE_TOTAL" "$suite_count"
            echo "Assigned snapshot suites:"
            sed 's/^/  /' "$TEST_WORKDIR/shard.txt"
            python3 .circleci/test_impact.py materialize \
              --selection "$CIRCLE_WORKING_DIRECTORY/ci-test-build/test-impact/selection.json" \
              --scheme StuffSnapshotTests \
              --assigned-file "$TEST_WORKDIR/shard.txt" \
              --output "$TEST_WORKDIR/selected-shard.txt"
            echo "Selected snapshot suites on this shard:"
            sed 's/^/  /' "$TEST_WORKDIR/selected-shard.txt"
            if [[ "$TEST_IMPACT_MODE" == enforce ]]; then
              cp "$TEST_WORKDIR/selected-shard.txt" "$TEST_WORKDIR/audit-shard.txt"
            else
              cp "$TEST_WORKDIR/shard.txt" "$TEST_WORKDIR/audit-shard.txt"
            fi
      - run_with_timeout:
          seconds: 3600
          command: |
            assigned_file="$TEST_WORKDIR/shard.txt"
            if [[ "$TEST_IMPACT_MODE" == enforce ]]; then
              assigned_file="$TEST_WORKDIR/selected-shard.txt"
              if [[ ! -s "$assigned_file" ]]; then
                echo "Compiler-index selection contains no suites on this shard; skipping simulator execution."
                exit 0
              fi
            fi
            ./test --snapshots --shared --os 27.0 --review --timings \
              --test-artifacts "$CIRCLE_WORKING_DIRECTORY/ci-test-build" \
              --only-file "$assigned_file" \
              --skip-architecture
      - export_test_results:
          test_workdir: test-output-snapshots
          output_directory: test-results-snapshots
      - audit_test_impact:
          scheme: StuffSnapshotTests
          junit: test-results-snapshots/results.xml
          output_directory: test-impact-snapshots
          assigned_file: test-output-snapshots/audit-shard.txt
          mode: << pipeline.parameters.test_impact_mode >>
      - run:
          name: Stage snapshot suite timings
          when: always
          command: |
            mkdir -p snapshot-timing-artifact
            cp test-results-snapshots/results.xml \
              "snapshot-timing-artifact/results-$CIRCLE_NODE_INDEX.xml"
      - store_artifacts:
          path: snapshot-timing-artifact
          destination: snapshot-timings
      - collect_diagnostics:
          test_workdir: test-output-snapshots
          artifact_name: snapshots
          include_snapshot_images: true

workflows:
  "iOS CI":
    when: >-
      (pipeline.event.name == "push" and pipeline.git.branch == "main") or
      pipeline.event.name == "pull_request" or
      pipeline.parameters.run_ios_ci == "true"
    jobs:
      - build-ios-tests:
          name: Build iOS Tests
      - test-ios:
          name: Build & Test (iOS)
          requires:
            - Build iOS Tests
      - snapshot:
          name: Snapshot Tests (iOS)
          requires:
            - Build iOS Tests
