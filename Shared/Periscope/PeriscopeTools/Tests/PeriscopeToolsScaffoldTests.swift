import PeriscopeTools
import Testing

/// Scaffold smoke test — replaced as the module's real suites land.
struct PeriscopeToolsScaffoldTests {
    @Test func moduleImports() {
        #expect(Bool(true))
    }
}
