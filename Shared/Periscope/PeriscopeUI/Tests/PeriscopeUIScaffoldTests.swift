import PeriscopeUI
import Testing

/// Scaffold smoke test — replaced as the module's real suites land.
struct PeriscopeUIScaffoldTests {
    @Test func moduleImports() {
        #expect(Bool(true))
    }
}
