// Generated by upgrade-where-backup; do not commit.

import Foundation
import Testing
@testable import WhereCore

struct BackupUpgradeVerificationTests {
    @Test func upgradedBackupDecodesAndLoadsAssets() throws {
        let url = URL(fileURLWithPath: __BACKUP_PATH__)

        let result = try BackupService().readArchive(at: url)

        #expect(result.archive.formatVersion == BackupArchive.currentFormatVersion)
        #expect(result.archive.samples.count == __SAMPLES_COUNT__)
        #expect(result.archive.evidence.count == __EVIDENCE_COUNT__)
        #expect(result.archive.manualDays.count == __MANUAL_DAYS_COUNT__)
        #expect(result.archive.dismissedIssues.count == __DISMISSED_ISSUES_COUNT__)
        #expect(result.archive.trackedRegions.count == __TRACKED_REGIONS_COUNT__)
        #expect(result.archive.primaryRegions.count == __PRIMARY_REGIONS_COUNT__)
        #expect(result.archive.recordingDeviceProfiles.count == __DEVICE_PROFILES_COUNT__)
        #expect(result.archive.recordingDeviceMetadataChanges.count == __DEVICE_CHANGES_COUNT__)
        #expect(result.archive.recordingDeviceRemovals.count == __DEVICE_REMOVALS_COUNT__)
        #expect(result.archive.assets.count == __ASSETS_COUNT__)
        #expect(result.blobs.count == __ASSETS_COUNT__)
    }
}
